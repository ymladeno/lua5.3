function createMat(R,C)
  local mt = {}
  for i = 1, R do
    local row = {}
    mt[i] = row
    for j = 1, C do
      row[j] = 0
    end
  end
  return mt
end

function createMat1(R,C)
end

function add(a,b)
  local c = {}
  for i = 1, #a do
    for k, va in pairs(a[i])
      
    for j = 1, C do
    end
  end
end

local m = createMat(2,3)
print(#m)